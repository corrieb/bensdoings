# Example Dockerfile to show how to use the static compose dind parent

# TODO: Figure out how to pre-populate with the image data ahead-of-time using docker-compose pull
#       Difficulty is running docker-in-docker during the build process

FROM bensdoings/dind-compose-dynamic

COPY docker-compose.yml /

